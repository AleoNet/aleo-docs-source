---
id: overview
title: Provable SDK
sidebar_label: Overview
sidebar_position: 1
---

import PackageEntry from '@site/src/components/PackageEntry';

Aleo allows users to privately transact through zero-knowledge enhanced programs. The [Provable SDK](https://github.com/provablehq/sdk) is a collection of JavaScript/Typescript libraries that provides the power to use these programs within the browser and all other levels of the web stack to build privacy-preserving applications.

:::info Network selection
`@provablehq/sdk` is split into per-network entry points (`/testnet.js`, `/mainnet.js`, `/dynamic.js`). Always import one of these explicitly rather than the bare package name — see [Network Selection](./getting-started.mdx#network-selection) in Getting Started for details.
:::

The SDK consists of three main libraries:

<PackageEntry
  name="sdk"
  platforms={[
    { label: 'Source', url: 'https://github.com/provablehq/sdk/tree/mainnet/sdk' },
    { label: 'NPM', url: 'https://www.npmjs.com/package/@provablehq/sdk' },
  ]}
  noTopPadding
>

The core SDK library that provides Javascript/Typescript tools for building on and interacting with the Aleo network.

</PackageEntry>

<PackageEntry
  name="wasm"
  platforms={[
    { label: 'Source', url: 'https://github.com/provablehq/sdk/tree/mainnet/wasm' },
    { label: 'NPM', url: 'https://www.npmjs.com/package/@provablehq/wasm' },
    { label: 'crates.io', url: 'https://crates.io/crates/aleo-wasm' },
  ]}
>

Aleo Wasm is a Rust crate which compiles Aleo code responsible for creating and executing zero knowledge programs into WebAssembly.

When compiled with `wasm-pack` JavaScript bindings are generated for the WebAssembly allowing Aleo zero-knowledge programs to be used in the browser and NodeJS.

</PackageEntry>

<PackageEntry
  name="create-leo-app"
  platforms={[
    { label: 'Source', url: 'https://github.com/ProvableHQ/sdk/tree/mainnet/create-leo-app' },
    { label: 'NPM', url: 'https://www.npmjs.com/package/create-leo-app' },
  ]}
  noBorder
  noBottomPadding
>

A set of templates for web app examples in common frameworks such as React. Developers looking to start with working examples should start here.

You can start with a template by running:

```bash
npm create leo-app@latest
```

</PackageEntry>
