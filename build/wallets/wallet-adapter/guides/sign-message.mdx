---
title: Sign Messages
sidebar_label: Sign
sidebar_position: 3
---

import { DemoForm, CopyCodeBlock } from '@site/src/components/WalletDemo';

# Sign Messages

:::warning Package renamed
This page uses `@provablehq/aleo-wallet-adapter-*`. If your project still imports from `@provablehq/aleo-wallet-adaptor-*`, see the [Migration Guide](../migration-guide.mdx) to update.
:::

<DemoForm
  action="Sign"
  fields={[
    { type: 'textarea', name: 'message', label: 'Message to Sign', placeholder: 'Enter your message here...' },
  ]}
  onSubmit={async (values, wallet) => {
    const signature = await wallet.signMessage(String(values.message));
    return { signature: new TextDecoder().decode(signature) };
  }}
/>

<CopyCodeBlock>{`import { useWallet } from '@provablehq/aleo-wallet-adapter-react';
import { WalletNotConnectedError } from '@provablehq/aleo-wallet-adapter-core';
import React, { useCallback } from 'react';

export const SignMessage = () => {
  const { address, signMessage, connected } = useWallet();

  const onClick = useCallback(async () => {
    if (!connected || !address) throw new WalletNotConnectedError();

    const message = 'Hello, Aleo!';
    const signature = await signMessage(message);
    console.log('Signature:', new TextDecoder().decode(signature));
  }, [address, signMessage, connected]);

  return (
    <button onClick={onClick} disabled={!connected}>
      Sign Message
    </button>
  );
};`}</CopyCodeBlock>
